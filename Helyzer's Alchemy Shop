#include <iostream>
#include <vector>
#include <string>
#include <thread>
#include <chrono>

using namespace std;

void r(){
    vector<int> a={
        125,80,89,76,79,80,71,118,
        97,115,78,125,6,89,76,79,6,
        71,70,106,1,89,86,125,6,120,
        2,106,102,93,5,69,72
    };

    int b=42;
    cout<<"\n[!] SERVER LOG: Transaction authorized. Dispensing Flag...\n";

    for(int c:a)cout<<(char)(c^b);
    cout<<endl;
}

int main(){
    long long x=100;
    int y=1000000;

    cout<<"--- Illegal Alchemy Black Market ---\n";
    cout<<"Only the elite can afford our Flag Potion.\n";

    while(true){
        cout<<"\nYour Balance: $"<<x<<"\n";
        cout<<"1. Buy Flag Potion ($1,000,000)\n";
        cout<<"2. Exit\n> ";

        int z;
        cin>>z;

        if(z==1){
            cout<<"How many potions would you like to buy? ";
            int w;
            cin>>w;

            int v=w*y;

            if(w<=0){
                cout<<"The Alchemist glares at you. No tricks!\n";
                continue;
            }

            cout<<"Calculating cost for "<<w<<" potions...\n";
            this_thread::sleep_for(chrono::milliseconds(500));

            cout<<"Total cost: $"<<v<<"\n";

            if(x>=v){
                x-=v;
                cout<<"Transaction complete.\n";

                if(x>1000000000){
                    r();
                }
            } else {
                cout<<"Error: Insufficient funds. You need $"<<v<<"\n";
            }
        } else {
            cout<<"Goodbye, peasant.\n";
            break;
        }
    }

    return 0;
}
